package com.andreasx42.quizstreamapi.service.mapper.impl;

/*
@Component
@AllArgsConstructor
public class QuizMapper implements IMapper<Quiz, QuizDto> {

    @Override
    public QuizDto mapFromEntity(Quiz todo) {
        return new QuizDto(
                todo.getId(),
                todo.getUser()
                        .getId(),
                todo.getName(),
                todo.getPriority(),
                todo.getStatus(),
                todo.getUntilDate());
    }

    @Override
    public Quiz mapToEntity(QuizDto todoDto) {
        return new Quiz(todoDto.name(), todoDto.priority(), todoDto.status(), todoDto.untilDate());
    }
}
*/